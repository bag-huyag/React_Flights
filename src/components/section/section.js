import Sort from '../sort'
import Tickets from '../tickets'

import './section.scss'

function Section() {
  return (
    <section className="section">
      <Sort />
      <Tickets />
    </section>
  )
}

export default Section
